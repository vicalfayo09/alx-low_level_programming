#include "main.h"
/**
 * main - check the code
 * Description: Prints half of a string
 * Return: Always 0(success)
 */
int main(void)
{
	char *str;

	str = "0123456789";
	puts_half(str);
	return (0);
}
