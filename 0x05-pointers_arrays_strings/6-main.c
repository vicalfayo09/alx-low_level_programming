#include "main.h"
/**
 * main - check the code
 * Description: Prints one char out of two of a string
 * Return: Always 0(success)
 */
int main(void)
{
	char *str;

	str = "0123456789";
	puts2(str);
	return (0);
}
